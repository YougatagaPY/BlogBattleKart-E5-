<!-- Header Start -->
<header class="navigation">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <nav class="navbar navbar-expand-lg p-0">
                    <a class="navbar-brand" href="{{path('app_home')}}">
                        <img src={{asset("images/BattleKartLogo.png")}}>
                    </a>

                    <button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbarsExample09" aria-controls="navbarsExample09" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="ion-android-menu"></span>
                    </button>

                    <div class="collapse navbar-collapse ml-auto" id="navbarsExample09">
                        <ul class="navbar-nav ml-auto" style="padding: 0; margin: 0; list-style: none;">
                            <!-- Menu Items -->
                            <li class="nav-item active" style="margin-right: 20px;">
                                <a class="nav-link" href="{{path('app_home')}}">Home</a>
                            </li>
                            <li class="nav-item active" style="margin-right: 20px;">
                                <a class="nav-link" href="{{path('app_article_index')}}">Article</a>
                            </li>                    
                            <li class="nav-item active" style="margin-right: 20px;">
                             {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                                <a class="nav-link" href="{{path('app_logout')}}">Logout</a>
                             {% else %}
                                <a class="nav-link" href="{{path('app_login')}}">Login</a>
                                </li>
                                <li class="nav-item active" style="margin-right: 20px;">
                                <a class="nav-link" href="{{path('app_register')}}">Register</a>
                                </li>
                             {% endif %}
                            </li>
                            {% if is_granted('ROLE_ADMIN') %}
                                <li class="nav-item active" style="margin-right: 20px;">
                                <a class="nav-link" href="{{path('admin')}}">AdminDash</a>
                                </li>
                            {% endif %}
                            <!-- Dropdown Menu -->
                            <div class="dropdown">
                                <button class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-lg shadow focus:outline-none focus:ring-2 focus:ring-blue-300 focus:ring-opacity-50 transition duration-300 ease-in-out dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
    Catégorie
</button>

                                <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                                    {{ render(controller('App\\Controller\\CategorieController::ListCategorie')) }}
                                </div>
                            </div>
                        </ul>
                    </div>
                    <!-- Search Bar -->
                    <div style="margin-left: 20px;">
                        {{ render(controller('App\\Controller\\SearchController::searchBar')) }}
                    </div>
                </nav>
            </div>
        </div>
    </div>
</header>
<!-- Header Close -->

